[project]
name = "rapidocr-sidecar"
version = "0.1.0"
description = "RapidOCR Python sidecar for the Electron OCR plugin"
requires-python = ">=3.9"
dependencies = [
  "rapidocr>=3",
  "onnxruntime",
]

[tool.uv]
package = false
